import { PrivacyActions } from "@/components/account-actions";
import { requireUser } from "@/lib/auth";
import { database, ensureDatabase } from "@/lib/database";

export const dynamic = "force-dynamic";
export default async function SettingsPage() { const user = await requireUser("/ayarlar"); await ensureDatabase(); const consents = (await database().prepare("SELECT analytics,marketing,created_at FROM cookie_consents WHERE user_id = ? ORDER BY created_at DESC LIMIT 10").bind(user.id).all<{ analytics: number; marketing: number; created_at: string }>()).results; return <div className="dashboard-page"><div className="page-heading"><span className="kicker dark">GİZLİLİK MERKEZİ</span><h1>Hesap ve veri ayarları</h1></div><div className="settings-grid"><section><h2>Kişisel bilgiler</h2><dl><div><dt>Ad soyad</dt><dd>{user.firstName} {user.lastName}</dd></div><div><dt>E-posta</dt><dd>{user.email}</dd></div></dl></section><section><h2>İzin geçmişi</h2>{consents.map((consent) => <div className="verification-row" key={consent.created_at}><span>{new Date(consent.created_at).toLocaleString("tr-TR")}</span><b>Analitik {consent.analytics ? "açık" : "kapalı"} · Pazarlama {consent.marketing ? "açık" : "kapalı"}</b></div>)}{consents.length === 0 && <p>Henüz kayıtlı tercih yok.</p>}</section><section><h2>Veri hakları</h2><p>Kişisel verilerinin bir kopyasını isteyebilir veya hesap silme talebi oluşturabilirsin.</p><PrivacyActions/></section></div></div>; }
