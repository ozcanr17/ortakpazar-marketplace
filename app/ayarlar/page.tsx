import { PrivacyActions } from "@/components/account-actions";
import { requireUser } from "@/lib/auth";
import { createSupabaseAdminClient } from "@/lib/supabase/server";

export const dynamic = "force-dynamic";
export default async function SettingsPage() { const user = await requireUser("/ayarlar"); const client = createSupabaseAdminClient(); const [{ data: profile }, { data: addresses }, { data: consents }] = await Promise.all([client.from("profiles").select("first_name,last_name,phone").eq("user_id", user.id).single(), client.from("addresses").select("id,title,city,district,address_line").eq("user_id", user.id), client.from("consent_history").select("category,granted,created_at").eq("user_id", user.id).order("created_at", { ascending: false }).limit(20)]); return <div className="dashboard-page"><div className="page-heading"><span className="kicker dark">GİZLİLİK MERKEZİ</span><h1>Hesap ve veri ayarları</h1></div><div className="settings-grid"><section><h2>Kişisel bilgiler</h2><dl><div><dt>Ad soyad</dt><dd>{profile ? `${profile.first_name} ${profile.last_name}` : "—"}</dd></div><div><dt>E-posta</dt><dd>{user.email}</dd></div><div><dt>Telefon</dt><dd>{profile?.phone ?? "Eklenmemiş"}</dd></div></dl></section><section><h2>Kayıtlı adresler</h2>{addresses?.map((address) => <div className="address-row" key={address.id}><b>{address.title}</b><span>{address.address_line}, {address.district}/{address.city}</span></div>)}{!addresses?.length && <p>Kayıtlı adres yok.</p>}</section><section><h2>İzin geçmişi</h2>{consents?.map((consent, index) => <div className="verification-row" key={`${consent.category}-${index}`}><span>{consent.category}</span><b>{consent.granted ? "İzin verildi" : "Reddedildi"}</b></div>)}</section><section><h2>Veri hakları</h2><p>Kişisel verilerinin bir kopyasını isteyebilir veya hesap silme talebi oluşturabilirsin.</p><PrivacyActions/></section></div></div>; }
