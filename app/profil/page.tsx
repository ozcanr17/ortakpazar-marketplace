import Link from "next/link";
import { logoutAction } from "@/app/actions/auth";
import { requireUser } from "@/lib/auth";
import { createSupabaseAdminClient } from "@/lib/supabase/server";

export const dynamic = "force-dynamic";
export default async function ProfilePage() { const user = await requireUser("/profil"); const client = createSupabaseAdminClient(); const [{ data: profile }, { data: seller }] = await Promise.all([client.from("profiles").select("first_name,last_name,phone,identity_verification_status,created_at").eq("user_id", user.id).single(), client.from("seller_profiles").select("display_name,seller_type,rating_basis_points,completed_sales,verification_status").eq("user_id", user.id).maybeSingle()]); return <div className="dashboard-page"><div className="profile-head"><div className="profile-avatar">{profile?.first_name?.charAt(0) ?? user.email.charAt(0).toUpperCase()}</div><div><span className="kicker dark">HESABIM</span><h1>{profile ? `${profile.first_name} ${profile.last_name}` : user.email}</h1><p>{user.email} · {user.role}</p></div></div><div className="profile-grid"><section><h2>Hızlı erişim</h2><Link href="/siparisler">Siparişlerim <span>→</span></Link><Link href="/sat">Yeni ilan <span>→</span></Link><Link href="/favoriler">Favorilerim <span>→</span></Link><Link href="/mesajlar">Mesajlarım <span>→</span></Link><Link href="/ayarlar">Hesap ayarları <span>→</span></Link></section><section><h2>Doğrulama</h2><div className="verification-row"><span>Kimlik doğrulama</span><b>{profile?.identity_verification_status ?? "NOT_STARTED"}</b></div><div className="verification-row"><span>Satıcı doğrulama</span><b>{seller?.verification_status ?? "NOT_STARTED"}</b></div><div className="verification-row"><span>Tamamlanan satış</span><b>{seller?.completed_sales ?? 0}</b></div></section></div><form action={logoutAction}><button className="button outline">Çıkış yap</button></form></div>; }
