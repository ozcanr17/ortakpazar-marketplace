import { requireUser } from "@/lib/auth";
import { database, ensureDatabase } from "@/lib/database";

export const dynamic = "force-dynamic";
export default async function MessagesPage() { const user = await requireUser("/mesajlar"); await ensureDatabase(); const messages = (await database().prepare("SELECT id,sender_id,receiver_id,body,created_at,read_at FROM messages WHERE sender_id = ? OR receiver_id = ? ORDER BY created_at DESC LIMIT 100").bind(user.id, user.id).all<{ id: string; sender_id: string; receiver_id: string; body: string; created_at: string; read_at: string | null }>()).results; return <div className="dashboard-page"><div className="page-heading"><span className="kicker dark">GÜVENLİ İLETİŞİM</span><h1>Mesajlar</h1><p>İletişim yalnızca ürün veya sipariş bağlamında gerçekleşir. Telefon ve e-posta bilgileri maskelenir.</p></div><div className="message-list">{messages.map((message) => <article key={message.id} className={message.sender_id === user.id ? "sent" : "received"}><small>{message.sender_id === user.id ? "Sen" : "Karşı taraf"} · {new Date(message.created_at).toLocaleString("tr-TR")}</small><p>{message.body}</p></article>)}{messages.length === 0 && <div className="empty-state"><h2>Henüz mesaj yok</h2><p>Ürün detayından satıcıya güvenli şekilde soru sorabilirsin.</p></div>}</div></div>; }
